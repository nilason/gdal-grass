repos:
    -   repo: https://github.com/pre-commit/mirrors-clang-format
        rev: 'v15.0.6'
        hooks:
        -   id: clang-format
            exclude: >
              (?x)^(
                libtiff/tif_fax3sm.c
              )
